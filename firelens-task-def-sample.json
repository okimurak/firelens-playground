{
  "containerDefinitions": [
    {
      "cpu": 0,
      "essential": true,
      "image": "{{ must_env `ECR_URI` }}:latest",
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-region": "ap-northeast-1",
          "awslogs-stream-prefix": "batch",
          "awslogs-group": "/ecs-scheduled-tasks/firelens-example"
        }
      },
      "mountPoints": [],
      "name": "firelens-sample",
      "portMappings": [],
      "volumesFrom": []
    }
  ],
  "cpu": "256",
  "executionRoleArn": "{{ must_env `EXECUTION_ROLE_ARN` }}",
  "family": "rev-proxy-production-v1",
  "memory": "512",
  "networkMode": "awsvpc",
  "placementConstraints": [],
  "requiresCompatibilities": ["FARGATE"],
  "taskRoleArn": "{{ must_env `TASK_ROLE_ARN` }}",
  "volumes": []
}
